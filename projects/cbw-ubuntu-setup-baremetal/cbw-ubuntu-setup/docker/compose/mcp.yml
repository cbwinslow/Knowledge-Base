services:
  # Add your MCP servers here. Example placeholder service.
  mcp-example:
    image: alpine:3.20
    command: ["sh","-c","echo 'Add MCP servers here' && sleep infinity"]
    restart: unless-stopped
