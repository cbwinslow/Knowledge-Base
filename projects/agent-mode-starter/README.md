# Agent Mode Starter Library (CBW)

A batteries-included template to create, schedule, and audit “Agent Mode” jobs.

## Quickstart
pyenv install -s 3.10.14 && pyenv local 3.10.14
pip install uv || python -m pip install uv
make install
make validate
make dryrun
